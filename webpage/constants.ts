import { RGBA } from "image-palette";

export const EMPTY_ARRAY = [];

export const EMPTY_OBJECT = {};

export const COUNTRY_CODES: Record<"Norway", "NO"> = { Norway: "NO" };

export const EMPTY_COLOR = { luma: -1, rgba: [0, 0, 0, 0] as RGBA, amount: 0 };
